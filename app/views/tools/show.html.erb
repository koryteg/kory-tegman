<p id="notice"><%= notice %></p>

<div id="tools">
	
	
	<h3><%= @tool.name %></h3>
		<%= image_tag @tool.image_url.to_s %>
</div>
	
<%= link_to 'Edit', edit_tool_path(@tool) %> |
<%= link_to 'Back', tools_path %>
